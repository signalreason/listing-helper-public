#!/bin/sh

set -eu

python -m app.migrate
exec python -m uvicorn app.main:app \
  --host 0.0.0.0 \
  --port "${PORT:-8080}" \
  --workers 1 \
  --no-access-log \
  --proxy-headers \
  --forwarded-allow-ips='*'
